begin
    let x be int;
    set x to 1 + 2 * 3;
    print x;
    set x to (1 + 2) * 3;
    print x;
    set x to 1 + 2 - 3 + 4;
    print x;
    set x to 2;
    print x;
    set x to (x + 1) * x;
    print x
end